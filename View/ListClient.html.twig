{% extends 'Master/MenuTemplate.html.twig' %}

{% block body %}

    <div class="container-fluid mt-3">
        <div class="row">
            <div class="col">
                <div class="card shadow-sm mb-4">
                    <div class="card-header bg-white">
                        <h1 class="h4 mb-0">
                            <i class="fa-solid fa-users me-2" aria-hidden="true"></i>
                            Clientes de Stripe
                        </h1>
                    </div>
                    <div class="card-body">
                        <form action="{{ asset('ListClient') }}?action=load" method="post">
                            <div class="row g-3 align-items-end">
                                <div class="col-md-5">
                                    <label class="form-label" for="sk_stripe_index">Cuenta de Stripe</label>
                                    <select id="sk_stripe_index" name="sk_stripe_index" class="form-select">
                                        {% for key,value in fsc.sks_stripe %}
                                            <option {% if(key == fsc.sk_stripe_index) %} selected {% endif %}
                                                    value="{{ key }}">{{ value.name }}</option>
                                        {% endfor %}
                                    </select>
                                </div>
                                <div class="col-md-5">
                                    <label class="form-label" for="stripe_customer_email">Email (opcional)</label>
                                    <input type="text" id="stripe_customer_email" name="stripe_customer_email"
                                           class="form-control" value="{{ fsc.stripe_customer_email }}"
                                           placeholder="Agrega un email si lo deseas"/>
                                </div>
                                <div class="col-md-2">
                                    <button onclick="muestraSpinner(true)" class="btn btn-info w-100" type="submit">
                                        <i class="fa-solid fa-magnifying-glass me-1" aria-hidden="true"></i> Consultar
                                    </button>
                                </div>
                            </div>
                        </form>
                    </div>
                </div>

                {% if(fsc.action=='load') %}
                    {% set res = fsc.clients %}
                    <div class="card shadow-sm">
                        <div class="card-header bg-white d-flex justify-content-between align-items-center">
                            <h2 class="h5 mb-0">
                                <i class="fa-solid fa-list me-2" aria-hidden="true"></i>
                                Resultados
                            </h2>
                            <span class="badge bg-primary rounded-pill">{{ res|length }}</span>
                        </div>
                        <div class="card-body">
                            <div class="table-responsive">
                                <table class="table table-striped table-hover align-middle mb-0">
                                    <thead class="table-light">
                                    <tr>
                                        <th>Id</th>
                                        <th>Email</th>
                                        <th>Cliente FS</th>
                                        <th class="text-end">Acciones</th>
                                    </tr>
                                    </thead>
                                    <tbody>
                                    {% for p in res %}
                                        <tr>
                                            <td><code>{{ p.id }}</code></td>
                                            <td>{{ p.email }}</td>
                                            <td>
                                                {% if p.fs_idClient %}
                                                    <a href="{{ asset('EditCliente') }}?code={{ p.fs_idClient }}"
                                                       target="_blank" title="Ver cliente">
                                                        <i class="fas fa-eye me-1" aria-hidden="true"></i>{{ p.fs_idClient }}
                                                    </a>
                                                {% else %}
                                                    <span class="text-muted">—</span>
                                                {% endif %}
                                            </td>
                                            <td class="text-end">
                                                <a class="btn btn-sm btn-outline-primary"
                                                   href="{{ asset('SelectClient') }}?stripe_customer_id={{ p.id }}&source=ListClient&action=changing"
                                                   title="Vincular cliente">
                                                    <i class="fas fa-plus" aria-hidden="true"></i>
                                                </a>
                                            </td>
                                        </tr>
                                    {% else %}
                                        <tr>
                                            <td colspan="4" class="text-center text-muted py-4">No hay clientes.</td>
                                        </tr>
                                    {% endfor %}
                                    </tbody>
                                </table>
                            </div>
                        </div>
                    </div>
                {% endif %}
            </div>
        </div>
    </div>

    {{ include('_spinner.html.twig') }}
{% endblock %}
